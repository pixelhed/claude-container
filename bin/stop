#!/bin/bash
## Host: Stop the container (preserves volumes)

source "$(dirname "$0")/compose-args"
docker compose $COMPOSE_FILES down
echo "✅ Stopped. Config and history preserved in Docker volumes."
